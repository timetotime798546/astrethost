<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state (Button is pressed down, 3D face shifts down) -->
    <item android:state_pressed="true">
        <layer-list>
            <item android:top="4dp">
                <shape android:shape="rectangle">
                    <solid android:color="@color/accent_dark" />
                    <corners android:radius="10dp" />
                </shape>
            </item>
        </layer-list>
    </item>
    <!-- Normal raised state -->
    <item>
        <layer-list>
            <!-- Shadow Extrusion Base -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/accent_dark" />
                    <corners android:radius="10dp" />
                </shape>
            </item>
            <!-- Raised Front Face (shifted upward by 4dp) -->
            <item android:bottom="4dp">
                <shape android:shape="rectangle">
                    <solid android:color="@color/accent" />
                    <corners android:radius="10dp" />
                    <stroke android:width="1dp" android:color="@color/accent_light" />
                </shape>
            </item>
        </layer-list>
    </item>
</selector>