\npackage com.studyplanner.app;\n\nimport android.app.Activity;\nimport android.os.Bundle;\nimport android.view.View;\nimport android.widget.Button;\nimport android.widget.LinearLayout;\nimport android.widget.TextView;\nimport java.util.ArrayList;\nimport java.util.List;\n\npublic class SubjectsActivity extends Activity {\n\n    private LinearLayout subjectsLayout;\n    private List<String> subjects = new ArrayList<String>();\n\n    @Override\n    protected void onCreate(Bundle savedInstanceState) {\n        super.onCreate(savedInstanceState);\n        setContentView(R.layout.activity_subjects);\n\n        subjectsLayout = (LinearLayout) findViewById(R.id.subjects_list_layout);\n        Button addSubjectButton = (Button) findViewById(R.id.add_subject_button);\n\n        addSubjectButton.setOnClickListener(new View.OnClickListener() {\n            @Override\n            public void onClick(View v) {\n                // In a real app, this would open a dialog or a new activity to add a subject\n                // For now, let's just add a placeholder subject\n                addSubject(\"New Subject \" + (subjects.size() + 1));\n            }\n        });\n\n        // Populate with initial subjects if any\n        if (subjects.isEmpty()) {\n            addSubject(\"Mathematics\");\n            addSubject(\"Physics\");\n            addSubject(\"Chemistry\");\n        }\n    }\n\n    private void addSubject(String subjectName) {\n        subjects.add(subjectName);\n        TextView subjectTextView = new TextView(this);\n        subjectTextView.setText(subjectName);\n        subjectTextView.setTextSize(18sp);\n        subjectTextView.setPadding(0, 8, 0, 8);\n        subjectsLayout.addView(subjectTextView);\n    }\n}\n\n