\npackage com.app.studyplanner;\n\nimport android.app.Activity;\nimport android.os.Bundle;\nimport android.view.View;\nimport android.widget.Button;\nimport android.widget.LinearLayout;\nimport android.widget.TextView;\nimport java.util.ArrayList;\nimport java.util.List;\n\npublic class TasksActivity extends Activity {\n\n    private LinearLayout tasksLayout;\n    private List<String> tasks = new ArrayList<String>();\n\n    @Override\n    protected void onCreate(Bundle savedInstanceState) {\n        super.onCreate(savedInstanceState);\n        setContentView(R.layout.activity_tasks);\n\n        tasksLayout = (LinearLayout) findViewById(R.id.tasks_list_layout);\n        Button addTaskButton = (Button) findViewById(R.id.add_task_button);\n\n        addTaskButton.setOnClickListener(new View.OnClickListener() {\n            @Override\n            public void onClick(View v) {\n                // In a real app, this would open a dialog or a new activity to add a task\n                // For now, let's just add a placeholder task\n                addTask(\"Complete Homework \" + (tasks.size() + 1));\n            }\n        });\n\n        if (tasks.isEmpty()) {\n            addTask(\"Read Chapter 5 (Mathematics)\");\n            addTask(\"Prepare for Physics Exam\");\n            addTask(\"Write Chemistry Report\");\n        }\n    }\n\n    private void addTask(String taskName) {\n        tasks.add(taskName);\n        TextView taskTextView = new TextView(this);\n        taskTextView.setText(taskName);\n        taskTextView.setTextSize(18f); // Changed 18sp to 18f\n        taskTextView.setPadding(0, 8, 0, 8);\n        tasksLayout.addView(taskTextView);\n    }\n}\n\n