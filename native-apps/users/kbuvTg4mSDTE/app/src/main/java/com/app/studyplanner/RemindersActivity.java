\npackage com.app.studyplanner;\n\nimport android.app.Activity;\nimport android.os.Bundle;\nimport android.view.View;\nimport android.widget.Button;\nimport android.widget.LinearLayout;\nimport android.widget.TextView;\nimport java.util.ArrayList;\nimport java.util.List;\n\npublic class RemindersActivity extends Activity {\n\n    private LinearLayout remindersLayout;\n    private List<String> reminders = new ArrayList<String>();\n\n    @Override\n    protected void onCreate(Bundle savedInstanceState) {\n        super.onCreate(savedInstanceState);\n        setContentView(R.layout.activity_reminders);\n\n        remindersLayout = (LinearLayout) findViewById(R.id.reminders_list_layout);\n        Button addReminderButton = (Button) findViewById(R.id.add_reminder_button);\n\n        addReminderButton.setOnClickListener(new View.OnClickListener() {\n            @Override\n            public void onClick(View v) {\n                // In a real app, this would open a dialog or a new activity to add a reminder\n                // For now, let's just add a placeholder reminder\n                addReminder(\"Reminder: Study Physics (Tomorrow 9 AM)\");\n            }\n        });\n\n        if (reminders.isEmpty()) {\n            addReminder(\"Physics Exam in 3 days\");\n            addReminder(\"Submit Chemistry Report by Friday\");\n        }\n    }\n\n    private void addReminder(String reminderText) {\n        reminders.add(reminderText);\n        TextView reminderTextView = new TextView(this);\n        reminderTextView.setText(reminderText);\n        reminderTextView.setTextSize(18f); // Changed 18sp to 18f\n        reminderTextView.setPadding(0, 8, 0, 8);\n        remindersLayout.addView(reminderTextView);\n    }\n}\n\n